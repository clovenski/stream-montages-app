apiVersion: apps/v1
kind: Deployment
metadata:
  name: jobs-repo
  namespace: stream-montages
spec:
  replicas: 1
  selector:
    matchLabels:
      app: jobs-repo
  template:
    metadata:
      labels:
        app: jobs-repo
    spec:
      containers:
      - name: jobs-repo
        image: jtengco-stream-montages-jobs-repo:0.0.1
        envFrom:
        - secretRef:
            name: jobs-repo-secrets
        resources:
          limits:
            cpu: "1"
            memory: "256Mi"
          requests:
            cpu: "0.5"

---

kind: Service
apiVersion: v1
metadata:
  name: jobs-repo-service
  namespace: stream-montages
spec:
  selector:
    app: jobs-repo
  type:  ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
